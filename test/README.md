# Test results

This folder stores the latest test run output. `npm test` writes `test-results.json` and `test-results.md` here. The JSON holds raw Vitest results; the MD file is a human-friendly summary (table of test cases and pass/fail). Commit these files to track test history in the repo.
